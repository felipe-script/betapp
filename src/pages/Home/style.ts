import styled from 'styled-components'
import tw from 'twin.macro'

export const Container = styled.div`
    ${tw`bg-gray-800 flex  h-screen flex-col`}
`

export const CardWrapper = styled.section`${tw`bg-gray-100 dark:bg-gray-800 py-10 px-12`}`

export const Title = styled.h1`${tw`text-gray-300 text-center mt-6`}`